# Food_recipe_API_using_flask

Access the API at http://localhost:5000.

API Endpoints:

GET /recipes: Retrieve all recipes.
GET /recipes/<recipe_id>: Retrieve a specific recipe.
POST /recipes: Add a new recipe.
